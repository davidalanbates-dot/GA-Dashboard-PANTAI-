// Dashboard configuration. This is the only thing you should ever need to
// edit — just the Sheet ID, already filled in below. No API key, no Google
// Cloud setup: the dashboard reads the Sheet through its public CSV export,
// which works as long as the Sheet is shared as "Anyone with the link".
window.DASHBOARD_CONFIG = {
  SHEET_ID: "12fmna96dAMXd7Jmk5B4g-XWM6ZAtIGoxlRnqtkhcm-s",
};
